{
  "name": "tweeter-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:shared && npm run copy:shared && npm run compile",
    "build:shared": "cd ../tweeter-shared && npm run compile",
    "copy:shared": "cp ../tweeter-shared/package.json layer/nodejs/node_modules/tweeter-shared/ && cp -r ../tweeter-shared/dist/. layer/nodejs/node_modules/tweeter-shared/dist/",
    "compile": "npm run clean && tsc",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "date-fns": "^3.3.1",
    "tweeter-shared": "file:../tweeter-shared",
    "uuid": "^9.0.1"
  }
}
